# 「云随想」完整功能设计方案

## Context

Little Claude 目前是一个桌面端 Claude Code GUI，用户量已达数百人。产品方向是将 MemoryWork 能力融入 Little Claude，让用户打开即有一套成型的记忆工作工具。

「云随想」是这个方向的 MVP 第一步——一个跨端的想法收集与同步模块。用户在移动端或桌面端随时记录灵感、语音、文件，所有内容云端同步，桌面端下次对话时可拉取使用。

---

## 一、产品定义

### 云随想是什么

一个轻量的跨端想法收集器。核心价值：**随时随地捕捉想法，回到桌面端时一切就绪。**

### Local-First 设计原则

云随想采用**本地优先**架构——所有核心功能不依赖服务器，离线即可使用。云同步是可选的增强能力。

```
Layer 0 · 本地模式（零配置，开箱即用）
├── 桌面端创建/编辑/删除/搜索随想
├── 引用到对话 / 保存对话为随想
├── 语音录制 + 实时转写（火山引擎 STT）
└── 数据存储在本地（SQLite）

Layer 1 · 云同步模式（配对后解锁）
├── 移动端 ↔ 桌面端双向同步
└── 云端备份
```

用户装完 Little Claude 就能直接使用云随想的本地功能，不需要注册、配对或联网。想跨端同步时再配对。

### 核心功能（移动端和桌面端一致）

| 功能 | 说明 |
|------|------|
| **文字记录** | 快速输入文字，支持简单 Markdown |
| **语音便签** | 录音时实时 STT 转写，保存语音+文字双份内容 |
| **小文件上传** | 图片、截图、代码片段、PDF（≤10MB/文件） |
| **云同步** | 新增/编辑/删除实时同步到所有端 |
| **搜索与标签** | 按关键词搜索、标签过滤 |

### 双向打通能力（桌面端）

| 功能 | 方向 | 说明 |
|------|------|------|
| **引用到对话** | 随想→对话 | 将随想条目作为上下文素材注入 Claude Code 对话 |
| **拖拽注入** | 随想→对话 | 拖拽随想卡片到 InputBar 输入框 |
| **保存为随想** | 对话→随想 | 对话中的消息右键「保存为随想」，同步到移动端 |
| **新消息提醒** | 双向 | 有新的移动端随想时，桌面端显示未读徽章 |

---

## 二、已确认的决策

| 决策项 | 选择 | 说明 |
|--------|------|------|
| **移动端形态** | 独立 APP | 使用 React Native (Expo) 开发，iOS + Android 双端 |
| **与对话关系** | 双向打通 | 随想可注入对话 + 对话中的内容也能保存为随想 |
| **用户身份** | 配对码 | 桌面端生成 6 位码，移动端输入绑定，零注册 |
| **语音转写** | 火山引擎大模型 ASR | WebSocket 流式，录音时实时转写，桌面端+移动端统一方案 |

### 为什么选 React Native (Expo)

- Little Claude 前端是 React + TypeScript，**技术栈完全一致**，可复用类型定义和部分逻辑
- Expo 简化了原生能力接入（录音、文件选择、推送通知），不需要碰 Xcode/Android Studio
- 可以通过 Expo Go 快速测试，不需要每次都构建原生包
- 上架 App Store / Google Play / 国内安卓市场

### 双向打通的具体含义

```
方向 1：随想 → 对话
- 云随想面板中点击「引用」或拖拽 → 随想内容注入到 InputBar
- Claude Code 可以读取随想内容作为上下文

方向 2：对话 → 随想
- 对话中的 AI 回复或用户消息，右键菜单「保存为随想」
- 保存时可选择添加标签、编辑备注
- 保存后在云随想面板和移动端 APP 中可见
```

---

## 三、数据模型

### Thought（随想条目）

```typescript
interface Thought {
  id: string;                    // UUID
  user_id: string;               // 配对 token（关联桌面端和移动端）

  // 内容
  content: string;               // 文字内容（Markdown）
  type: 'text' | 'voice' | 'file' | 'mixed';  // 条目类型

  // 语音便签
  voice_url?: string;            // 语音文件 COS 路径
  voice_duration?: number;       // 语音时长（秒）
  voice_transcript?: string;     // 火山引擎 STT 实时转写文字（录音时同步生成）

  // 附件
  attachments?: Attachment[];

  // 元数据
  tags?: string[];               // 用户标签
  source: 'mobile' | 'desktop';  // 来源设备
  is_read: boolean;              // 桌面端是否已读

  // 时间
  created_at: number;            // Unix timestamp
  updated_at: number;
  deleted_at?: number;           // 软删除
}

interface Attachment {
  id: string;
  filename: string;
  file_url: string;              // COS 路径
  file_size: number;             // 字节
  mime_type: string;
  thumbnail_url?: string;        // 图片缩略图
}
```

### UserPair（设备配对）

```typescript
interface UserPair {
  id: string;                    // 配对 token
  pair_code: string;             // 6 位配对码（临时，配对后失效）
  desktop_device_id: string;     // 桌面端设备标识
  mobile_device_id?: string;     // 移动端设备标识（配对后写入）

  created_at: number;
  paired_at?: number;            // 配对成功时间
  last_sync_at?: number;         // 最后同步时间
}
```

---

## 四、技术架构

### 整体架构图

```
┌──────────────────┐          ┌──────────────────────┐
│  React Native APP │          │  Little Claude 桌面端     │
│  (iOS + Android) │          │  (云随想面板)          │
│                  │          │                      │
│  · 文字输入       │          │  · 文字输入            │
│  · 语音录制+STT   │◄────────►│  · 语音录制+STT        │
│  · 文件上传       │   云同步   │  · 随想列表            │
│  · 随想列表       │          │  · 引用到对话          │
│  · 对话摘要查看   │          │  · 保存对话为随想       │
│                  │          │  · 未读提醒            │
└────────┬─────────┘          └──────────┬───────────┘
         │                               │
         │          HTTPS API            │
         └───────────────┬───────────────┘
                         │
               ┌─────────▼─────────┐
               │  腾讯云后端         │
               │                   │
               │  云函数 SCF (API)   │
               │        │          │
               │  ┌─────▼──────┐   │
               │  │ COS 对象存储 │   │ ← 语音、文件
               │  └────────────┘   │
               │  ┌────────────┐   │
               │  │ 云数据库     │   │ ← 随想元数据、配对信息
               │  │(MongoDB)   │   │
               │  └────────────┘   │
               └───────────────────┘
                         │
         ┌───────────────┴───────────────┐
         │  火山引擎（双端直连）            │
         │  · 大模型流式语音识别 (WebSocket) │
         └───────────────────────────────┘
```

### 共享代码策略

桌面端和移动端可以共享以下代码（通过 npm 私有包或 monorepo）：

```
shared/
├── types.ts              ← Thought, Attachment, UserPair 类型定义
├── cloud-api.ts          ← API 客户端（axios 封装）
├── volcengine-stt.ts     ← 火山引擎 ASR WebSocket 客户端（双端共享）
├── sync-logic.ts         ← 同步队列、冲突解决逻辑
└── validators.ts         ← 输入校验规则
```

### 后端 API 设计

```
POST   /api/pair/generate        ← 桌面端生成配对码
POST   /api/pair/bind            ← 移动端输入配对码绑定
GET    /api/pair/status           ← 检查配对状态

GET    /api/thoughts              ← 获取随想列表（分页、筛选）
POST   /api/thoughts              ← 创建随想
PUT    /api/thoughts/:id          ← 更新随想
DELETE /api/thoughts/:id          ← 删除随想（软删除）
GET    /api/thoughts/unread       ← 获取未读数量

POST   /api/upload/presign        ← 获取 COS 预签名上传 URL
```

### 认证机制

```
配对流程：
1. 桌面端请求 POST /api/pair/generate → 返回 { pair_code: "482916", token: "xxx" }
2. 桌面端显示 6 位配对码，用户在手机上输入
3. 移动端请求 POST /api/pair/bind { pair_code: "482916" } → 返回 { token: "xxx" }
4. 双端都持有 token，后续请求在 Header 中带 Authorization: Bearer <token>

安全措施：
- 配对码 5 分钟过期
- 3 次错误输入后 15 分钟冷却
- Token 有效期 90 天，可刷新
- 所有 API 走 HTTPS
```

---

## 五、移动端设计（React Native APP）

### 技术栈

| 组件 | 方案 | 说明 |
|------|------|------|
| 框架 | **Expo (React Native)** | 与桌面端 React+TS 技术栈一致，可复用类型定义 |
| UI 库 | React Native Paper 或 NativeWind (Tailwind) | NativeWind 可复用桌面端 Tailwind 样式经验 |
| 语音录制 | `expo-av` | 支持录音 + 播放 |
| 语音转写 STT | 火山引擎大模型流式语音识别 | WebSocket 实时转写，支持中英混说+方言 |
| 文件选择 | `expo-document-picker` + `expo-image-picker` | 图片、文件选择 |
| 文件上传 | COS SDK + 预签名直传 | 直传 COS，不经后端中转 |
| 网络请求 | axios + token 拦截器 | 与桌面端 cloud-api.ts 可共享逻辑 |
| 本地存储 | `expo-secure-store` + AsyncStorage | Token 安全存储 + 离线缓存 |
| 推送通知 | `expo-notifications` | 桌面端有新随想时推送（可选） |

### 项目结构

```
tokenicode-mobile/                ← 独立仓库
├── app/                          ← Expo Router 页面
│   ├── (tabs)/                   ← Tab 导航
│   │   ├── index.tsx             ← 随想列表（主页）
│   │   ├── create.tsx            ← 快速新建
│   │   └── settings.tsx          ← 设置
│   ├── thought/[id].tsx          ← 随想详情
│   ├── pair.tsx                  ← 配对绑定页
│   └── _layout.tsx              ← 根布局
├── components/
│   ├── ThoughtCard.tsx
│   ├── VoiceRecorder.tsx
│   ├── FileUploader.tsx
│   └── TagSelector.tsx
├── lib/
│   ├── cloud-api.ts             ← 云端 API 客户端（可与桌面端共享类型）
│   ├── volcengine-stt.ts        ← 火山引擎 ASR WebSocket 客户端（与桌面端共享）
│   ├── auth.ts                  ← Token 管理
│   └── sync.ts                  ← 同步逻辑
├── stores/
│   └── thoughtsStore.ts         ← Zustand（与桌面端结构对齐）
└── shared/                       ← 与桌面端共享的类型定义
    └── types.ts                  ← Thought, Attachment, UserPair 等
```

### APP 页面设计

**主页（随想列表）**

```
┌─────────────────────────────┐
│  ☁ 云随想          ⚙ 设置  │
├─────────────────────────────┤
│  🔍 搜索随想...              │
├─────────────────────────────┤
│  📅 今天                     │
│  ┌───────────────────────┐  │
│  │ 关于新功能的一些想法     │  │
│  │ 刚才在地铁上突然想到... │  │
│  │ #产品 · 3分钟前     🖥  │  │ ← 🖥 = 来自桌面端
│  └───────────────────────┘  │
│  ┌───────────────────────┐  │
│  │ 🎙 语音备忘 (0:42)      │  │
│  │ "用户反馈说界面太复杂"  │  │
│  │ ▶ 播放                  │  │
│  │ #反馈 · 1小时前     📱  │  │ ← 📱 = 来自移动端
│  └───────────────────────┘  │
│  ┌───────────────────────┐  │
│  │ 📎 截图.png (1.2MB)    │  │
│  │ 竞品分析截图            │  │
│  │ #调研 · 今天 14:30  📱  │  │
│  └───────────────────────┘  │
│                             │
│  📅 昨天                     │
│  ┌───────────────────────┐  │
│  │ [对话摘要] API性能优化   │  │ ← 来自桌面端「保存为随想」
│  │ Claude建议用缓存分层... │  │
│  │ #开发 · 昨天 16:20  🖥  │  │
│  └───────────────────────┘  │
│                             │
├─────────────────────────────┤
│          ┌────┐             │
│          │ ＋ │             │  ← FAB 悬浮按钮
│          └────┘             │
└─────────────────────────────┘
```

**快速新建（底部弹起）**

```
┌─────────────────────────────┐
│                             │
│  ┌───────────────────────┐  │
│  │                       │  │
│  │  在这里写下你的想法...  │  │
│  │                       │  │
│  └───────────────────────┘  │
│                             │
│  附件：                      │
│  ┌─────┐ ┌─────┐            │
│  │ img │ │ doc │  + 添加    │
│  └─────┘ └─────┘            │
│                             │
│  标签：                      │
│  [ #产品 ] [ #想法 ] + 添加  │
│                             │
├─────────────────────────────┤
│  🎙 语音  📷 拍照  📎 文件  │  ← 底部工具栏
│                    [ 保存 ] │
└─────────────────────────────┘
```

**配对页**

```
┌─────────────────────────────┐
│           连接桌面端          │
│                             │
│  请在桌面端 Little Claude 中    │
│  打开「云随想」面板           │
│  获取 6 位配对码              │
│                             │
│    ┌─┐ ┌─┐ ┌─┐ ┌─┐ ┌─┐ ┌─┐│
│    │ │ │ │ │ │ │ │ │ │ │ ││  ← 6 位输入框
│    └─┘ └─┘ └─┘ └─┘ └─┘ └─┘│
│                             │
│        [ 确认配对 ]          │
│                             │
│  配对后即可双向同步随想内容   │
└─────────────────────────────┘
```

### 上架渠道

| 平台 | 要求 | 说明 |
|------|------|------|
| iOS App Store | Apple 开发者账号 ($99/年) | 审核 1~3 天 |
| Google Play | Google 开发者账号 ($25 一次性) | 审核 1~3 天 |
| 国内安卓市场 | 企业资质 + 软著 | 应用宝、华为、小米等 |

> MVP 阶段可先走 iOS + Google Play，国内安卓市场后续补上。
> 或使用 Expo 的 OTA 更新绕过频繁审核。

---

## 六、桌面端设计（Little Claude 云随想面板）

### 集成位置

在 **SecondaryPanel**（右侧面板）新增标签页「云随想」，与现有的 Files、Skills 标签页并列。

### 需修改的文件

```
src/
├── stores/
│   ├── settingsStore.ts         ← 扩展 SecondaryPanelTab 类型
│   └── thoughtsStore.ts         ← 新建：云随想状态管理
│
├── components/
│   ├── layout/
│   │   └── SecondaryPanel.tsx   ← 注册新标签页
│   │
│   └── thoughts/                ← 新建目录
│       ├── CloudThoughtsPanel.tsx  ← 主面板（列表 + 搜索 + 操作）
│       ├── ThoughtCard.tsx         ← 单条随想卡片
│       ├── ThoughtEditor.tsx       ← 新建/编辑随想
│       ├── ThoughtDetail.tsx       ← 随想详情展开
│       ├── PairSetup.tsx           ← 配对码生成/绑定界面
│       └── VoiceRecorder.tsx       ← 桌面端语音录制组件
│
├── lib/
│   ├── tauri-bridge.ts          ← 新增云随想 IPC 命令
│   ├── i18n.ts                  ← 新增翻译键
│   └── cloud-api.ts             ← 新建：云端 API 客户端
│
└── hooks/
    └── useCloudSync.ts          ← 新建：同步逻辑钩子
```

### 桌面端面板 UI

```
┌──────────────────────────────┐
│  Files  Skills  ☁ 云随想      │ ← 标签页栏
├──────────────────────────────┤
│  🔍 搜索...        + 新建    │
├──────────────────────────────┤
│  ● 2 条新随想                 │ ← 未读提醒
│  ┌────────────────────────┐  │
│  │ 关于新功能的一些想法    📱│  │ ← 📱 = 来自移动端
│  │ 刚才在地铁上突然想到... │  │
│  │ #产品 · 3分钟前         │  │
│  │ ┌────┐                 │  │
│  │ │引用│  ← 引用到对话    │  │
│  │ └────┘                 │  │
│  └────────────────────────┘  │
│  ┌────────────────────────┐  │
│  │ 🎙️ 语音备忘 (0:42)  📱│  │
│  │ "用户反馈说界面太复杂" │  │
│  │ ▶️ 播放                │  │
│  │ ┌────┐                 │  │
│  │ │引用│                 │  │
│  │ └────┘                 │  │
│  └────────────────────────┘  │
│                              │
│  ── 昨天 ──                  │
│  ┌────────────────────────┐  │
│  │ 📎 截图.png (1.2MB)  🖥│  │ ← 🖥 = 来自桌面端
│  │ 竞品分析截图            │  │
│  │ ┌────┐                 │  │
│  │ │引用│                 │  │
│  │ └────┘                 │  │
│  └────────────────────────┘  │
├──────────────────────────────┤
│  配对状态: ✅ 已连接          │
│  最后同步: 2 分钟前           │
└──────────────────────────────┘
```

### 方向 1：随想 → 对话（引用）

两种方式：
1. **点击「引用」按钮** → 随想内容以引用块格式插入 InputBar
2. **拖拽卡片** → 拖到 ChatPanel 的 InputBar 区域松手

注入格式：
```
[云随想引用]
---
{随想文字内容}
{如有附件: [附件名称](本地缓存路径)}
{如有语音转写: 语音转写: "xxx"}
---
```

### 方向 2：对话 → 随想（保存）

在 **MessageBubble** 组件的右键菜单（或 hover 操作栏）新增「保存为随想」选项：

```
交互流程：
1. 用户在对话中 hover 某条消息 → 出现操作栏
2. 点击「保存为随想」图标
3. 弹出迷你编辑框：
   - 自动填充消息内容摘要
   - 可添加标签、编辑备注
   - 标记来源为「对话摘要」
4. 点击保存 → 创建随想条目 → 同步到云端 → 移动端可见
```

需修改的现有组件：
- `src/components/chat/MessageBubble.tsx` — 新增操作按钮
- 或在现有的 hover 操作栏中加一个「保存」图标

### 配对码界面

首次使用时显示配对引导：

```
┌──────────────────────────────┐
│  ☁ 连接移动端                 │
│                              │
│  在手机上打开「云随想」APP     │
│  进入配对页面                  │
│  输入以下配对码：              │
│                              │
│      ┌─────────────┐        │
│      │  4 8 2 9 1 6 │        │
│      └─────────────┘        │
│      4:32 后过期              │
│                              │
│  [ 刷新配对码 ]              │
│                              │
│  还没有 APP？                 │
│  扫描下方二维码下载 ↓         │
│  ┌─────┐                    │
│  │ QR  │                    │
│  └─────┘                    │
└──────────────────────────────┘
```

---

## 七、同步机制

### 同步策略

```
拉取策略（桌面端）：
1. 应用启动时拉取最新随想列表
2. 每 30 秒轮询新消息（GET /api/thoughts/unread）
3. 手动刷新按钮

推送策略（双端）：
1. 创建/编辑/删除时立即调 API
2. 失败时存入本地队列，下次联网重试

冲突处理（MVP 简化）：
- 随想条目不支持多端同时编辑同一条
- 以最后写入时间戳为准（Last Write Wins）
- 删除操作优先于编辑操作
```

### 离线支持

```
移动端（React Native APP）：
- 新建随想时先存 AsyncStorage
- 联网后自动同步到云端
- 离线草稿标记「待同步」状态

桌面端（Little Claude）：
- 随想列表缓存在本地（thoughtsStore 持久化）
- 新建/编辑操作先写本地再推云端
- 网络恢复后自动重试队列
```

---

## 八、语音便签技术方案（火山引擎大模型 ASR）

### 工作流程

```
用户按下录音 → expo-av 开始录音（16kHz/16bit）
      │
      ├─→ 音频流通过 WebSocket 实时发送到火山引擎 ASR
      │         │
      │         └─→ 实时返回转写文字，UI 上同步显示
      │
用户松手停止录音
      │
      ├─→ 保存原始语音文件（上传 COS）
      ├─→ 保存最终转写文字（写入 content / voice_transcript）
      └─→ 创建 type: 'voice' 的随想条目
```

### 接入参数

| 参数 | 值 | 说明 |
|------|-----|------|
| 协议 | WebSocket | 双端统一 |
| 识别模式 | `bigmodel_nostream` | 流式输入，录完出结果，准确度最高 |
| 采样率 | 16000 Hz | expo-av 录音配置 |
| 位深 | 16 bit | 单声道 |
| 单包大小 | 200ms | 性能最优 |
| `enable_ddc` | true | 语义顺滑，去掉停顿词（嗯、啊、那个） |
| `enable_punc` | true | 自动标点 |

### 鉴权

```
鉴权走后端代理，不在客户端暴露 Key：
1. 客户端请求后端获取临时 WebSocket 连接信息
2. 后端签发带时效的连接参数
3. 客户端直连火山引擎 WebSocket（音频流不经后端中转）

Header：
- X-Api-App-Key: APP ID
- X-Api-Access-Key: 访问令牌
- X-Api-Resource-Id: 资源类型
- X-Api-Connect-Id: UUID（追踪用）
```

### 支持语言

默认空值即支持：中文、英文、中英混说、上海话、闽南语、四川话、陕西话、粤语。无需用户手动选择。

### 桌面端接入

桌面端通过 Tauri Rust 后端或前端 WebSocket 直连火山引擎 ASR，复用同一套鉴权和协议逻辑。

### 共享代码

```
shared/
├── volcengine-stt.ts         ← WebSocket 连接、二进制协议封装、转写回调
└── types.ts                  ← STT 相关类型定义
```

移动端和桌面端共享 `volcengine-stt.ts`，录音组件各自实现（expo-av / Web Audio API）。

---

## 九、成本估算（500 用户）

### 运营成本（月度）

| 项目 | 月费 | 说明 |
|------|------|------|
| 腾讯云函数 SCF | ¥0~30 | 免费额度 10 万次/月 |
| 腾讯云 COS | ¥5~20 | 存储 + 流量 |
| 云数据库 MongoDB | ¥25~50 | 最小规格 |
| 火山引擎 ASR | 待确认 | 按小时计费，有资源包阶梯价 |
| **月运营合计** | **¥30~100+/月** | ASR 费用取决于实际用量 |

### 一次性成本

| 项目 | 费用 | 说明 |
|------|------|------|
| Apple 开发者账号 | $99/年 (≈¥720) | iOS 上架必须 |
| Google Play 开发者 | $25 一次性 (≈¥180) | Android 上架 |
| 国内安卓市场（后续） | 软著费 ¥300~500 | 应用宝等需要软件著作权 |
| **一次性合计** | **≈¥1,000~1,400** | |

> MVP 阶段基础设施成本较低，后续可通过增值功能覆盖。

---

## 十、开发路线图

### Phase 1：基础骨架 + 共享层（1.5 周）
- [ ] 共享类型定义（Thought, Attachment, UserPair）
- [ ] 后端 API 搭建（云函数 + COS + MongoDB）
- [ ] 配对码生成与绑定流程
- [ ] 桌面端 thoughtsStore + CloudThoughtsPanel 空壳
- [ ] 移动端 Expo 项目初始化 + 页面框架

### Phase 2：核心功能（2 周）
- [ ] 移动端 APP：随想列表 + 新建（纯文字）
- [ ] 桌面端：随想列表 + 新建 + 同步拉取
- [ ] 文件上传（COS 预签名直传）
- [ ] 未读提醒徽章
- [ ] 「引用到对话」功能（按钮 + 拖拽）

### Phase 3：语音便签 + 双向打通（1.5 周）
- [ ] 火山引擎 ASR 接入（WebSocket 客户端 + 鉴权代理）
- [ ] 移动端语音录制 + 实时转写（expo-av + volcengine-stt）
- [ ] 桌面端语音录制 + 实时转写
- [ ] 语音便签播放（原始语音回听）
- [ ] 「保存对话为随想」功能（MessageBubble 操作栏）
- [ ] 对话摘要类型随想在移动端的展示

### Phase 4：完善 + 上架（1 周）
- [ ] 搜索、标签过滤
- [ ] 离线支持与同步优化
- [ ] iOS App Store + Google Play 上架

**总周期预估：5~6 周**

---

## 十一、风险与注意事项

1. **App Store 审核**：iOS 审核对「笔记类 + 需要配合桌面端使用」的 APP 可能有疑问，需要准备清晰的 App 描述和演示视频
2. **国内安卓分发**：各市场（华为、小米、应用宝等）需要软件著作权，申请周期 1~2 个月。MVP 阶段可先走 APK 直接下载 + Google Play
3. **语音文件大小**：60 秒语音约 1MB，需要设置录制时长上限（建议 5 分钟）
4. **COS 流量费**：如果用户频繁下载语音/文件，流量费可能超预期。可设置文件本地缓存策略
5. **配对码安全**：6 位数字码空间较小（100 万种），需要防暴力破解（限速 + 冷却）
6. **数据隐私**：用户随想可能包含敏感内容，服务端存储需加密，隐私政策需明确
7. **Expo 限制**：部分原生能力（如后台录音、蓝牙等）可能需要 bare workflow 而非 managed workflow。语音录制和文件上传在 managed workflow 下可行
