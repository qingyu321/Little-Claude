/**
 * useRemoteSession — stub for Little Claude (remote sessions not supported).
 * Her has the full implementation; this is a no-op to satisfy imports.
 */
export function useRemoteSession() {
  // No-op: Little Claude does not support remote sessions (Telegram, Feishu, etc.)
}
